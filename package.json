{
  "name": "ai-skill-html-planner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun scripts/build.ts",
    "ci": "oxlint . --deny-warnings -f unix && oxfmt --check src cli templates site --no-error-on-unmatched-pattern && bun run validate && bun run ci:build-check",
    "ci:build-check": "bun run build && git diff --exit-code dist",
    "dev": "vite build -c vite.config.ts --watch",
    "validate": "bun cli/planner.ts validate templates examples site"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "^26.2.0",
    "htmlparser2": "^12.0.0",
    "oxfmt": "^0.63.0",
    "oxlint": "^1.78.0",
    "playwright": "^1.62.1",
    "tailwindcss": "^4.3.3",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
